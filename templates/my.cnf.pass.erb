<% %w(mysql client mysqldump mysqladmin mysqlcheck).each do |section| %>
[<%= section -%>]
user=root
host=localhost
<% unless scope.lookupvar('mariadb_galera::root_password') == 'UNSET' -%>
password='<%= scope.lookupvar('mariadb_galera::root_password') %>'
<% end -%>
socket=<%= @client_options['client']['socket'] %>
<% end %>
